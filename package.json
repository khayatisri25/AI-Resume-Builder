{
  "name": "ai-resume-builder-root",
  "version": "1.0.0",
  "description": "Root orchestrator for AI-powered Resume Builder",
  "scripts": {
    "install-all": "npm install --prefix frontend && pip install -r requirements.txt",
    "backend": "uvicorn backend.app:app --reload --port 8000",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
